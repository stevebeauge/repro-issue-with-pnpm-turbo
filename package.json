{
    "name": "mycorp",
    "private": true,
    "scripts": {
        "build-all" : "turbo build",
        "clean" : "rimraf .turbo && pnpm -r run clean"
    },

    "devDependencies": {
        "pnpm": "^8.7.0",
        "rimraf": "^5.0.1",
        "turbo": "^1.10.14"
    }
}